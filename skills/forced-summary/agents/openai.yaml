interface:
  display_name: "Forced Summary"
  short_description: "Insert endpoint summaries without grounding review"
  default_prompt: "Use $forced-summary to write the local endpoint summary into eligible Obsidian notes without semantic review, then stage successful notes in AI Processed."
